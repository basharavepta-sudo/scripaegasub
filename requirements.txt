# Required for Ollama backend (recommended)
requests>=2.25.0

# Required for Gemini backend
google-generativeai>=0.3.0

# Optional: for local transformers backend (requires GPU with 24GB+ VRAM)
# transformers>=4.35.0
# torch>=2.0.0
# accelerate>=0.24.0
